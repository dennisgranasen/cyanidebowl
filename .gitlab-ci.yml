stages:
  - build
  - test
  - package
  - deploy
  - release

variables:
  MAVEN_OPTS: -Dmaven.repo.local=.m2/repository

cache:
  paths:
    - .m2/repository
    - target

maven-build:
  stage: build
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  rules:
    - if: $CI_COMMIT_BRANCH =~ /^release-.*/
      when: never
    - if: $CI_COMMIT_BRANCH
      when: always
  script: "mvn -Ddockerfile.skip clean compile --batch-mode"

maven-test:
  stage: test
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  rules:
    - if: $CI_COMMIT_BRANCH =~ /^release-/
      when: never
    - if: $CI_COMMIT_BRANCH
      when: always
  script: "mvn test --fail-at-end --batch-mode"
  artifacts:
    reports:
      junit:
        - "**/target/surefire-reports/TEST-*.xml"
        - "**/target/junit.xml"

maven-package:
  stage: package
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  services:
    - docker:dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_DRIVER: overlay2
  only:
    - /^dev$/
  script: "mvn -DskipTests=true install"

maven-deploy-snapshot:
  stage: deploy
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  services:
    - docker:dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_DRIVER: overlay2
  only:
    - /^dev$/
  when: manual
  before_script:
    - version=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec
  script:
    - "mvn -DskipTests=true -Ddockerfile.skip deploy -s ci_settings.xml"
    - echo "Logging in to registry..."
    - docker login registry.gitlab.com
    - echo "Tagging docker images..."
    - for project in frontend backend discord-bot; do docker tag warpscores/warpscores-${project}:${version} registry.gitlab.com/warp-scores/warp-scores-${project}:${version}; done
    - echo "Pushing docker images..."
    - for project in frontend backend discord-bot; do docker push registry.gitlab.com/warp-scores/warp-scores-${project}:${version}; done

maven-release:
  stage: release
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  services:
    - docker:dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_DRIVER: overlay2
  only:
    - /^main$/
  script: "mvn -DskipTests=true release:prepare release:perform --batch-mode -s ci_settings.xml"

maven-deploy:
  stage: package
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  services:
    - docker:dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_DRIVER: overlay2
  only:
    - /^main$/
  when: manual
  before_script:
    - version=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec
  script:
    - "mvn -DskipTests=true -Ddockerfile.skip deploy -s ci_settings.xml"
    - echo "Logging in to registry..."
    - docker login registry.gitlab.com
    - echo "Tagging docker images..."
    - for project in frontend backend discord-bot; do docker tag warpscores/warpscores-${project}:${version} registry.gitlab.com/warp-scores/warp-scores-${project}:${version}; done
    - echo "Pushing docker images..."
    - for project in frontend backend discord-bot; do docker push registry.gitlab.com/warp-scores/warp-scores-${project}:${version}; done
