stages:
  - build
  - test
  - package
  - deploy

variables:
  MAVEN_OPTS: -Dmaven.repo.local=.m2/repository

cache:
  paths:
    - .m2/repository
    - target

maven-build:
  stage: build
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  script: "mvn -Ddockerfile.skip clean compile -B"

maven-test:
  stage: test
  image: registry.hub.docker.com/library/maven:3.8-openjdk-17
  script: "mvn -Ddockerfile.skip test -fae -B"
  artifacts:
    reports:
      junit:
        - "**/target/surefire-reports/TEST-*.xml"
