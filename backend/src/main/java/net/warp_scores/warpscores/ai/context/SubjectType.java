package net.warp_scores.warpscores.ai.context;

/** Canonical subject kinds used for deterministic AI context retrieval. */
public enum SubjectType {
    GENERAL,
    USER,
    TEAM,
    COACH_IDENTITY,
    PLAYER,
    MATCH,
    ARTICLE,
    COMPETITION,
    LEAGUE_SYSTEM,
    TOPIC
}
